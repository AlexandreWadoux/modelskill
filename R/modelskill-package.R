#' Quantitative model skill indices and diagrams
#'
#' Compare numeric predictions against paired observations with
#' [model_metrics()] and individual metrics such as [rmse()] and [ccc()].
#' Validate predictive uncertainty using [uncertainty_metrics()], [qcp()],
#' [pit()], and [crps()]. Visualize interval and quantile calibration with
#' [gg_coverage()] and [gg_qcp()]. Complementary graphical
#' diagnostics include [gg_taylor()], [gg_solar()] and [gg_target()]. All plots
#' return ordinary ggplot objects. No spatial data class is required.
#'
#' @keywords internal
"_PACKAGE"
