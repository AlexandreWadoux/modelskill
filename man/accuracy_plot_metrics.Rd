% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accuracy_plot_metrics.R
\name{accuracy_plot_metrics}
\alias{accuracy_plot_metrics}
\title{Summarize accuracy-plot calibration}
\usage{
accuracy_plot_metrics(
  obs,
  lower = NULL,
  upper = NULL,
  level = NULL,
  pred = NULL,
  predictive_sd = NULL,
  levels = NULL,
  na.rm = TRUE
)
}
\arguments{
\item{obs}{Numeric observation vector.}

\item{lower, upper}{Named lists of interval-bound vectors. Names must be
nominal levels such as \code{"0.50"} or \code{"0.95"}. A single pair of numeric
vectors is also accepted when \code{level} is supplied.}

\item{level}{Nominal level for a single numeric lower/upper pair. It must be
\code{NULL} when named lists are supplied.}

\item{pred, predictive_sd}{Optional predictive means and predictive standard
deviations. When supplied together, normal central intervals are generated
for \code{levels}; do not also supply \code{lower} or \code{upper}.}

\item{levels}{Nominal levels for normal predictive distributions. Defaults to
every percentage from 1\% to 99\%.}

\item{na.rm}{Logical; remove incomplete interval triplets?}
}
\value{
One-row data frame with absolute and directional calibration areas.
}
\description{
Derives calibration summaries from a PICP reliability plot, also called an
accuracy plot. It summarizes PICP evaluated over multiple nominal interval
levels. \code{absolute_deviation} is the trapezoidal integral of
the absolute vertical distance from the 1:1 line. \code{over_uncertainty} and
\code{under_uncertainty} are the corresponding areas above and below the line;
their percentages partition absolute deviation when it is non-zero. For a
normal predictive distribution, supply \code{pred} and \code{predictive_sd} to assess
the default 1\%--99\% curve, as in Goovaerts (2001).
}
\examples{
accuracy_plot_metrics(1:3, pred = c(1, 2, 4), predictive_sd = rep(1, 3))
}
\references{
Goovaerts, P. (2001). Geostatistical modelling of uncertainty in
soil science. \emph{Geoderma}, 103, 3-26. \url{doi:10.1016/S0016-7061(01)00067-2}
}
