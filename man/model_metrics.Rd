% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_metrics.R
\name{model_metrics}
\alias{model_metrics}
\title{Calculate prediction-validation metrics for one or more models}
\usage{
model_metrics(mods, obs, na.rm = TRUE, extended = FALSE, digits = NULL)
}
\arguments{
\item{mods}{Numeric vector, list of numeric vectors, or numeric matrix/data
frame with one model per column. Rows match \code{obs} in order. Supplied model
names must be unique; missing names are generated.}

\item{obs}{A numeric observation vector.}

\item{na.rm}{Logical; whether incomplete observation-prediction pairs should
be removed. The default is \code{TRUE}. If \code{FALSE}, incomplete pairs result in
missing statistics rather than being silently removed.}

\item{extended}{Logical; include additional robust, scale-normalised,
percentage, agreement, and KGE (2009) metrics?}

\item{digits}{Integer or \code{NULL}. If supplied, round numeric results to this
many digits (0 to 22). \code{NULL} retains full numerical precision.}
}
\value{
A base data frame with one row per model and canonical columns
\code{model}, \code{bias}, \code{mae}, \code{mse}, \code{rmse}, \code{nrmse}, \code{crmse}, \code{correlation},
\code{r2}, \code{R2}, \code{sd_ratio}, \code{ccc}, and \code{Cb}. With \code{extended = TRUE}, adds
\code{mdae}, \code{rpd}, \code{rpiq}, \code{sep}, \code{rer}, \code{mape}, \code{mpe}, \code{smape}, \code{msle},
\code{rmsle}, \code{rae}, \code{rrmse}, \code{willmott_d}, and \code{kge} (KGE (2009)).
Duplicate columns ME, MAE, RMSE, r, nse, NSE, MEC, and rhoC have been
removed; use bias, mae, rmse, correlation, R2, and ccc instead.
}
\description{
Applies the individual prediction metrics to one or more prediction vectors.
Each statistic is returned once. Efficiency is named \code{R2}; standalone
\code{\link[=nse]{nse()}} and \code{\link[=mec]{mec()}} remain aliases. See the linked metric help pages for
equations, references, and interpretation.
}
\details{
Errors are observation minus prediction. See \code{\link[=bias]{bias()}}, \code{\link[=crmse]{crmse()}}, \code{\link[=nse]{nse()}},
and \code{\link[=ccc]{ccc()}} for definitions and interpretation.

Errors are observation minus prediction: negative ME indicates
overprediction. ME, MAE and RMSE have the input units. NSE is one minus the
ratio of squared error to the observation sum of squares about its mean.
Zero is the observation-mean benchmark and negative values are worse.
Concordance uses population variances (divisor n); rhoC = r * Cb.
Missing pairs are removed separately for each model, so comparisons may use
different subsets. NA and NaN are missing; infinite values are rejected.
Repeated observations are retained with equal weight.
With fewer than two pairs, correlation, efficiency and concordance are NA.
Constant inputs return NA for r and r2 because Pearson correlation is
undefined. Constant observations give NA efficiency
and concordance. Constant predictions with varying observations give zero
Cb and rhoC (the continuous limiting value). All-missing models return NA.
}
\section{Bias correction factor}{

\code{Cb} is Lin's bias correction factor, using population SDs and means:
\deqn{C_b = \frac{2\sigma_o\sigma_p}{\sigma_o^2+\sigma_p^2+(\mu_o-\mu_p)^2}.}
It ranges from zero to one; one indicates equal means and SDs. It does
not measure correlation. For defined correlation, CCC equals r times Cb.
See \code{\link[=ccc]{ccc()}} and Lin (1989), \url{doi:10.2307/2532051}.
}

\examples{
obs <- c(1, 2, 3, 4, 5)
preds <- list(model_a = c(1, 2, 3, 4, 5),
              model_b = c(2, 2, 3, 4, 4))
model_metrics(preds, obs)
}
\references{
Wadoux, A. M. J.-C., Walvoort, D. J. J., and Brus, D. J. (2022). An
integrated approach for the evaluation of quantitative soil maps through
Taylor and solar diagrams. \emph{Geoderma}, 405, 115332.
\url{doi:10.1016/j.geoderma.2021.115332}
}
\seealso{
\code{\link[=diagram_stats]{diagram_stats()}}, \code{\link[=gg_taylor]{gg_taylor()}}, \code{\link[=gg_solar]{gg_solar()}}, \code{\link[=gg_target]{gg_target()}}
}
