% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{model_metrics}
\alias{model_metrics}
\title{Calculate quantitative model-skill indices}
\usage{
model_metrics(mods, obs, na.rm = TRUE, digits = NULL)
}
\arguments{
\item{mods}{A numeric prediction vector, or a named list of numeric
prediction vectors.}

\item{obs}{A numeric observation vector.}

\item{na.rm}{Logical; whether incomplete observation-prediction pairs should
be removed. The default is \code{TRUE}. If \code{FALSE}, incomplete pairs result in
missing statistics rather than being silently removed.}

\item{digits}{Integer or \code{NULL}. If supplied, round numeric results to this
many digits. \code{NULL} (the default) retains full numerical precision.}
}
\value{
A data frame with one row per model and columns \code{ME}, \code{MAE}, \code{RMSE},
\code{r}, \code{r2}, \code{NSE}, \code{rhoC}, and \code{Cb}.
}
\description{
Calculates error, correlation, efficiency, and concordance statistics for
one or more prediction vectors against a common observation vector. Missing
values are removed by complete pairs, so every statistic is calculated from
the same locations.
}
\details{
The definitions follow Wadoux, Walvoort, and Brus (2022), including mean
error (ME), mean absolute error (MAE), root mean square error (RMSE),
Pearson correlation (\code{r}), modelling efficiency coefficient (\code{NSE}), and
Lin's concordance correlation components (\code{rhoC} and \code{Cb}).
}
\examples{
obs <- c(1, 2, 3, 4, 5)
preds <- list(model_a = c(1, 2, 3, 4, 5),
              model_b = c(2, 2, 3, 4, 4))
model_metrics(preds, obs)
}
\references{
Wadoux, A. M. J.-C., Walvoort, D. J. J., and Brus, D. J. (2022). An
integrated approach for the evaluation of quantitative soil maps through
Taylor and solar diagrams. \emph{Geoderma}, 405, 115332.
\url{https://doi.org/10.1016/j.geoderma.2021.115332}
}
